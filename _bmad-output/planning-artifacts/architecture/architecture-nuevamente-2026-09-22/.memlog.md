---
scope: Sistema NuevaMente (Hackathon ONE G10)
purpose: build-substrate, solution design and diagrams
altitude: feature
updated: 2026-09-22T20:00
---

- (direction) User chose Coaching path (1) and requested Architecture Spine plus visual Solution Design document with architecture diagrams
- (constraint) PRD fixed stack: Python 3.12, Streamlit UI, Voyage AI (voyage-multilingual-2, 1024d), ChromaDB embedded vector store, Gemini Flash default via OpenAI-compatible abstraction, OCI Object Storage Always Free with OCI_MOCK mode, Pydantic v2 schemas
- (direction) User proposed incorporating LangGraph / LangChain for pipeline orchestration
- (decision) Adopt LangGraph StateGraph for pipeline orchestration with node stages: ingest -> retrieve -> generate -> evaluate -> persist, using conditional edge for retry on fidelity score < threshold
- (decision) Streamlit state invariant: st.session_state['active_package'] acts as single source of truth for rendered views; services cached with @st.cache_resource; UI interactions (quiz clicks, flashcard flips) do not re-run pipeline
- (decision) ChromaDB collection strategy: isolated collection per document hash doc_{sha256[:12]} in persistent storage, preventing cross-contamination and providing free embedding cache
- (decision) Quality Audit: LLM-as-a-judge node at temp=0.0 validates output against retrieved chunks, outputs EvaluacionCalidad, triggers self-reflection feedback loop to node_generate up to 2 retries if score < threshold
- (decision) Storage Abstraction: StoragePort interface with OCIObjectStorageAdapter (OCI SDK Always Free) and MockLocalStorageAdapter (OCI_MOCK=true, writes to ./mock_oci_storage/), sharing identical Pydantic AlmacenamientoOCIInfo schema
- (version) Stack versions pinned: Python 3.12, streamlit>=1.38, langgraph>=0.2, langchain-core>=0.3, voyageai>=0.2.3, chromadb>=0.5.5, pydantic>=2.8, pypdf>=4.3, oci>=2.130
- (decision) Adopt modular repository tree: core/, domain/, services/, pipeline/ (LangGraph), ui/, tests/, mock_oci_storage/
- (event) spine finalized
- (decision) Party review findings applied: AD-5 hash clarified to post-normalization text, AD-4 retry UX convention added, AD-2 LLM self-evaluation risk documented
